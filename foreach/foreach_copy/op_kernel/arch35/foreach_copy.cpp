/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file foreach_copy.cpp
 * \brief Kernel entry point for foreach_copy operator (arch35).
 */

#include "foreach_copy_simt.h"

using namespace AscendC;

template <uint32_t schMode>
__global__ __aicore__ void foreach_copy(
    GM_ADDR x, GM_ADDR y, GM_ADDR workspace, GM_ADDR tiling)
{
    REGISTER_TILING_DEFAULT(ForeachCopyTilingData);
    GET_TILING_DATA_WITH_STRUCT(ForeachCopyTilingData, tilingData, tiling);

    if (tilingData.totalElements <= 0) {
        return;
    }

    if constexpr (schMode == 0) {
        NsForeachCopy::ProcessSame<half>(x, y, tiling);
    } else if constexpr (schMode == 1) {
        NsForeachCopy::ProcessSame<float>(x, y, tiling);
    } else if constexpr (schMode == 2) {
        NsForeachCopy::ProcessSame<int32_t>(x, y, tiling);
    } else if constexpr (schMode == 3) {
        NsForeachCopy::ProcessSame<bfloat16_t>(x, y, tiling);
    } else if constexpr (schMode == 4) {
        NsForeachCopy::ProcessSame<uint8_t>(x, y, tiling);
    } else if constexpr (schMode == 5) {
        NsForeachCopy::ProcessSame<uint32_t>(x, y, tiling);
    } else if constexpr (schMode == 6) {
        NsForeachCopy::ProcessSame<int16_t>(x, y, tiling);
    } else if constexpr (schMode == 7) {
        NsForeachCopy::ProcessSame<uint16_t>(x, y, tiling);
    } else if constexpr (schMode == 8) {
        NsForeachCopy::ProcessSame<int8_t>(x, y, tiling);
    } else if constexpr (schMode == 9) {
        NsForeachCopy::ProcessSame<bool>(x, y, tiling);
    } else if constexpr (schMode == 10) {
        NsForeachCopy::ProcessSame<int64_t>(x, y, tiling);
    } else if constexpr (schMode == 11) {
        NsForeachCopy::ProcessSame<double>(x, y, tiling);
    } else if constexpr (schMode == 12) {
        NsForeachCopy::ProcessCast<float, bfloat16_t>(x, y, tiling);
    } else if constexpr (schMode == 13) {
        NsForeachCopy::ProcessCast<float, half>(x, y, tiling);
    } else if constexpr (schMode == 14) {
        NsForeachCopy::ProcessCast<half, float>(x, y, tiling);
    } else if constexpr (schMode == 15) {
        NsForeachCopy::ProcessCast<bfloat16_t, float>(x, y, tiling);
    }
}
