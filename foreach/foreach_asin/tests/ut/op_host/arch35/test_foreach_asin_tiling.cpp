/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */
/* Generated By CANNBot */

#include <iostream>
#include <gtest/gtest.h>
#include "log/log.h"
#include "kernel_run_context_facker.h"
#include "register/op_impl_registry.h"
#include "exe_graph/runtime/storage_format.h"
#include "exe_graph/runtime/storage_shape.h"
#include "platform/platform_infos_def.h"
#include "test_cube_util.h"
#include "ut_op_util.h"
#include "../../../../op_kernel/arch35/foreach_asin_tiling_data.h"
#include "../../../../op_kernel/arch35/foreach_asin_tiling_key.h"

using namespace ut_util;

class ForeachAsinTilingTest : public testing::Test {
protected:
    static void SetUpTestCase() { std::cout << "ForeachAsinTilingTest SetUp" << std::endl; }
    static void TearDownTestCase() { std::cout << "ForeachAsinTilingTest TearDown" << std::endl; }
};

TEST_F(ForeachAsinTilingTest, test_tiling_float16)
{
    fe::PlatFormInfos pf;
    std::map<std::string, std::string> soc, ai, intr;
    GetPlatFormInfos(R"({"hardware_info":{"UB_SIZE":253952,"CORE_NUM":64}})", soc, ai, intr);
    pf.Init();

    struct ForeachAsinCompileInfo {
        int64_t coreNum;
        int64_t ubSize;
    } ci = {64, 253952};
    auto opType = std::string("ForeachAsin");
    auto tilingFn = gert::OpImplRegistry::GetInstance().GetOpImpl(opType.c_str())->tiling;
    auto tilingParseFn = gert::OpImplRegistry::GetInstance().GetOpImpl(opType.c_str())->tiling_parse;
    ASSERT_NE(tilingFn, nullptr);
    ASSERT_NE(tilingParseFn, nullptr);

    std::string ciStr = R"({"device_id":null})";
    auto kh = gert::KernelRunContextFaker()
                  .KernelIONum(2, 1)
                  .Inputs({const_cast<char*>(ciStr.c_str()), reinterpret_cast<void*>(&pf)})
                  .Outputs({&ci})
                  .Build();
    kh.GetContext<gert::TilingParseContext>()->GetPlatformInfo()->Init();
    kh.GetContext<gert::TilingParseContext>()->GetPlatformInfo()->SetPlatformRes("SoCInfo", soc);
    kh.GetContext<gert::TilingParseContext>()->GetPlatformInfo()->SetPlatformRes("AICoreSpec", ai);
    kh.GetContext<gert::TilingParseContext>()->GetPlatformInfo()->SetCoreNumByCoreType("AICore");
    kh.GetContext<gert::TilingParseContext>()->GetPlatformInfo()->SetPlatformRes("AICoreintrinsicDtypeMap", intr);
    ASSERT_EQ(tilingParseFn(kh.GetContext<gert::KernelContext>()), ge::GRAPH_SUCCESS);

    auto param = gert::TilingData::CreateCap(4096);
    ASSERT_NE(param, nullptr);
    auto workspace = gert::ContinuousVector::Create<size_t>(4096);
    auto ws = reinterpret_cast<gert::ContinuousVector*>(workspace.get());
    gert::StorageShape inputs = {{3, 6, 5}, {3, 6, 5}};
    auto holder = gert::TilingContextFaker()
                      .NodeIoNum(1, 1)
                      .IrInstanceNum({1})
                      .InputShapes({&inputs})
                      .OutputShapes({&inputs})
                      .CompileInfo(&ci)
                      .PlatformInfo(reinterpret_cast<char*>(&pf))
                      .NodeInputTd(0, ge::DT_FLOAT16, ge::FORMAT_ND, ge::FORMAT_ND)
                      .NodeOutputTd(0, ge::DT_FLOAT16, ge::FORMAT_ND, ge::FORMAT_ND)
                      .TilingData(param.get())
                      .Workspace(ws)
                      .Build();
    auto tilingCtx = holder.GetContext<gert::TilingContext>();
    ASSERT_NE(tilingCtx->GetPlatformInfo(), nullptr);
    tilingCtx->GetPlatformInfo()->SetPlatformRes("SoCInfo", soc);
    tilingCtx->GetPlatformInfo()->SetPlatformRes("AICoreSpec", ai);
    tilingCtx->GetPlatformInfo()->SetCoreNumByCoreType("AICore");
    tilingCtx->GetPlatformInfo()->SetPlatformRes("AICoreintrinsicDtypeMap", intr);

    EXPECT_EQ(tilingFn(tilingCtx), ge::GRAPH_SUCCESS);
    ASSERT_EQ(tilingCtx->GetTilingKey(), GET_TPL_TILING_KEY(FOREACH_ASIN_TPL_DTYPE_FP16));
}

TEST_F(ForeachAsinTilingTest, test_tiling_float32)
{
    fe::PlatFormInfos pf;
    std::map<std::string, std::string> soc, ai, intr;
    GetPlatFormInfos(R"({"hardware_info":{"UB_SIZE":253952,"CORE_NUM":64}})", soc, ai, intr);
    pf.Init();

    struct ForeachAsinCompileInfo {
        int64_t coreNum;
        int64_t ubSize;
    } ci = {64, 253952};
    auto opType = std::string("ForeachAsin");
    auto tilingFn = gert::OpImplRegistry::GetInstance().GetOpImpl(opType.c_str())->tiling;
    auto tilingParseFn = gert::OpImplRegistry::GetInstance().GetOpImpl(opType.c_str())->tiling_parse;
    ASSERT_NE(tilingFn, nullptr);
    ASSERT_NE(tilingParseFn, nullptr);

    std::string ciStr = R"({"device_id":null})";
    auto kh = gert::KernelRunContextFaker()
                  .KernelIONum(2, 1)
                  .Inputs({const_cast<char*>(ciStr.c_str()), reinterpret_cast<void*>(&pf)})
                  .Outputs({&ci})
                  .Build();
    kh.GetContext<gert::TilingParseContext>()->GetPlatformInfo()->Init();
    kh.GetContext<gert::TilingParseContext>()->GetPlatformInfo()->SetPlatformRes("SoCInfo", soc);
    kh.GetContext<gert::TilingParseContext>()->GetPlatformInfo()->SetPlatformRes("AICoreSpec", ai);
    kh.GetContext<gert::TilingParseContext>()->GetPlatformInfo()->SetCoreNumByCoreType("AICore");
    kh.GetContext<gert::TilingParseContext>()->GetPlatformInfo()->SetPlatformRes("AICoreintrinsicDtypeMap", intr);
    ASSERT_EQ(tilingParseFn(kh.GetContext<gert::KernelContext>()), ge::GRAPH_SUCCESS);

    auto param = gert::TilingData::CreateCap(4096);
    ASSERT_NE(param, nullptr);
    auto workspace = gert::ContinuousVector::Create<size_t>(4096);
    auto ws = reinterpret_cast<gert::ContinuousVector*>(workspace.get());
    gert::StorageShape inputs = {{3, 6, 5}, {3, 6, 5}};
    auto holder = gert::TilingContextFaker()
                      .NodeIoNum(1, 1)
                      .IrInstanceNum({1})
                      .InputShapes({&inputs})
                      .OutputShapes({&inputs})
                      .CompileInfo(&ci)
                      .PlatformInfo(reinterpret_cast<char*>(&pf))
                      .NodeInputTd(0, ge::DT_FLOAT, ge::FORMAT_ND, ge::FORMAT_ND)
                      .NodeOutputTd(0, ge::DT_FLOAT, ge::FORMAT_ND, ge::FORMAT_ND)
                      .TilingData(param.get())
                      .Workspace(ws)
                      .Build();
    auto tilingCtx = holder.GetContext<gert::TilingContext>();
    ASSERT_NE(tilingCtx->GetPlatformInfo(), nullptr);
    tilingCtx->GetPlatformInfo()->SetPlatformRes("SoCInfo", soc);
    tilingCtx->GetPlatformInfo()->SetPlatformRes("AICoreSpec", ai);
    tilingCtx->GetPlatformInfo()->SetCoreNumByCoreType("AICore");
    tilingCtx->GetPlatformInfo()->SetPlatformRes("AICoreintrinsicDtypeMap", intr);

    EXPECT_EQ(tilingFn(tilingCtx), ge::GRAPH_SUCCESS);
    ASSERT_EQ(tilingCtx->GetTilingKey(), GET_TPL_TILING_KEY(FOREACH_ASIN_TPL_DTYPE_FP32));
}

TEST_F(ForeachAsinTilingTest, test_tiling_bfloat16)
{
    fe::PlatFormInfos pf;
    std::map<std::string, std::string> soc, ai, intr;
    GetPlatFormInfos(R"({"hardware_info":{"UB_SIZE":253952,"CORE_NUM":64}})", soc, ai, intr);
    pf.Init();

    struct ForeachAsinCompileInfo {
        int64_t coreNum;
        int64_t ubSize;
    } ci = {64, 253952};
    auto opType = std::string("ForeachAsin");
    auto tilingFn = gert::OpImplRegistry::GetInstance().GetOpImpl(opType.c_str())->tiling;
    auto tilingParseFn = gert::OpImplRegistry::GetInstance().GetOpImpl(opType.c_str())->tiling_parse;
    ASSERT_NE(tilingFn, nullptr);
    ASSERT_NE(tilingParseFn, nullptr);

    std::string ciStr = R"({"device_id":null})";
    auto kh = gert::KernelRunContextFaker()
                  .KernelIONum(2, 1)
                  .Inputs({const_cast<char*>(ciStr.c_str()), reinterpret_cast<void*>(&pf)})
                  .Outputs({&ci})
                  .Build();
    kh.GetContext<gert::TilingParseContext>()->GetPlatformInfo()->Init();
    kh.GetContext<gert::TilingParseContext>()->GetPlatformInfo()->SetPlatformRes("SoCInfo", soc);
    kh.GetContext<gert::TilingParseContext>()->GetPlatformInfo()->SetPlatformRes("AICoreSpec", ai);
    kh.GetContext<gert::TilingParseContext>()->GetPlatformInfo()->SetCoreNumByCoreType("AICore");
    kh.GetContext<gert::TilingParseContext>()->GetPlatformInfo()->SetPlatformRes("AICoreintrinsicDtypeMap", intr);
    ASSERT_EQ(tilingParseFn(kh.GetContext<gert::KernelContext>()), ge::GRAPH_SUCCESS);

    auto param = gert::TilingData::CreateCap(4096);
    ASSERT_NE(param, nullptr);
    auto workspace = gert::ContinuousVector::Create<size_t>(4096);
    auto ws = reinterpret_cast<gert::ContinuousVector*>(workspace.get());
    gert::StorageShape inputs = {{3, 6, 5}, {3, 6, 5}};
    auto holder = gert::TilingContextFaker()
                      .NodeIoNum(1, 1)
                      .IrInstanceNum({1})
                      .InputShapes({&inputs})
                      .OutputShapes({&inputs})
                      .CompileInfo(&ci)
                      .PlatformInfo(reinterpret_cast<char*>(&pf))
                      .NodeInputTd(0, ge::DT_BF16, ge::FORMAT_ND, ge::FORMAT_ND)
                      .NodeOutputTd(0, ge::DT_BF16, ge::FORMAT_ND, ge::FORMAT_ND)
                      .TilingData(param.get())
                      .Workspace(ws)
                      .Build();
    auto tilingCtx = holder.GetContext<gert::TilingContext>();
    ASSERT_NE(tilingCtx->GetPlatformInfo(), nullptr);
    tilingCtx->GetPlatformInfo()->SetPlatformRes("SoCInfo", soc);
    tilingCtx->GetPlatformInfo()->SetPlatformRes("AICoreSpec", ai);
    tilingCtx->GetPlatformInfo()->SetCoreNumByCoreType("AICore");
    tilingCtx->GetPlatformInfo()->SetPlatformRes("AICoreintrinsicDtypeMap", intr);

    EXPECT_EQ(tilingFn(tilingCtx), ge::GRAPH_SUCCESS);
    ASSERT_EQ(tilingCtx->GetTilingKey(), GET_TPL_TILING_KEY(FOREACH_ASIN_TPL_DTYPE_BF16));
}
