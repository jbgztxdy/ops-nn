/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file foreach_addcdiv_scalar_tiling_arch35.cpp
 * \brief Tiling implementation for foreach_addcdiv_scalar operator (arch35/Ascend950).
 *        Computes per-core tensor assignment using AssignDataToEachCore algorithm.
 *        Scalar value is stored in TilingData as scalarVal (MDE 3.1).
 */

#include <vector>
#include <algorithm>
#include <cstring>
#include "register/op_def_registry.h"
#include "tiling/tiling_api.h"
#include "platform/platform_info.h"
#include "log/log.h"
#include "foreach_addcdiv_scalar_tiling_arch35.h"

using namespace AscendC;

namespace optiling {

constexpr int64_t DCACHE_SIZE = 128 * 1024;
constexpr int64_t SINGLE_CORE_MIN_ELEMENTS = 1024;
constexpr int64_t ALIGN_SIZE = 32;
constexpr int64_t INPUT_X1_IDX = 0;

static uint64_t GetTilingKeyByDtype(ge::DataType dtype)
{
    switch (dtype) {
        case ge::DT_FLOAT16:
            return 0;
        case ge::DT_FLOAT:
            return 1;
        case ge::DT_BF16:
            return 2;
        default:
            return 0;
    }
}

static ge::graphStatus GetPlatformInfoFallback(gert::TilingContext* context, int64_t& coreNum, int64_t& ubSize)
{
    auto compileInfo = reinterpret_cast<const ForeachAddcdivScalarCompileInfo*>(context->GetCompileInfo());
    if (compileInfo != nullptr && compileInfo->coreNum > 0 && compileInfo->ubSize > 0) {
        coreNum = compileInfo->coreNum;
        ubSize = compileInfo->ubSize;
        return ge::GRAPH_SUCCESS;
    }
    fe::PlatFormInfos* platformInfoPtr = context->GetPlatformInfo();
    if (platformInfoPtr != nullptr) {
        auto ascendcPlatform = platform_ascendc::PlatformAscendC(platformInfoPtr);
        coreNum = ascendcPlatform.GetCoreNumAiv();
        uint64_t ub = 0;
        ascendcPlatform.GetCoreMemSize(platform_ascendc::CoreMemType::UB, ub);
        ubSize = static_cast<int64_t>(ub);
        if (coreNum > 0 && ubSize > 0) {
            return ge::GRAPH_SUCCESS;
        }
    }
    return ge::GRAPH_FAILED;
}

static void AssignDataToEachCore(ForeachAddcdivScalarTilingDataHost& tilingDataHost, int64_t needCoreNum,
                                 int64_t dataTypeSize)
{
    int64_t elementsPerBlock = ALIGN_SIZE / dataTypeSize;
    int64_t totalDataCount = tilingDataHost.totalDataCount;
    int64_t blockCount = (totalDataCount + elementsPerBlock - 1) / elementsPerBlock;
    if (blockCount == 0) {
        blockCount = 1;
    }

    int64_t perCoreBlockCount = blockCount / needCoreNum;
    int64_t remainder = blockCount % needCoreNum;

    uint16_t coreIndex = 0;
    int64_t cursorPosition = 0;
    int64_t dataCount = 0;
    tilingDataHost.tensorStartList[coreIndex] = 0;
    tilingDataHost.tensorStartOffsetList[coreIndex] = 0;

    for (uint16_t i = 0; i < static_cast<uint16_t>(tilingDataHost.tensorCount); i++) {
        int64_t curCmpCount = perCoreBlockCount * elementsPerBlock;
        if (remainder > 0 && coreIndex < static_cast<uint16_t>(remainder)) {
            curCmpCount += elementsPerBlock;
        }
        int64_t tempCount = tilingDataHost.tensorDataCountList[i] - cursorPosition;

        if (dataCount + tempCount < curCmpCount) {
            dataCount += tempCount;
            cursorPosition = 0;
            continue;
        }

        tilingDataHost.tensorEndList[coreIndex] = i;
        cursorPosition = cursorPosition + curCmpCount - dataCount;
        tilingDataHost.tensorEndOffsetList[coreIndex] = cursorPosition - 1;
        dataCount = 0;
        coreIndex++;

        if (cursorPosition < tilingDataHost.tensorDataCountList[i]) {
            tilingDataHost.tensorStartList[coreIndex] = i;
            tilingDataHost.tensorStartOffsetList[coreIndex] = cursorPosition;
            --i;
        } else if (coreIndex != static_cast<uint16_t>(needCoreNum)) {
            tilingDataHost.tensorStartList[coreIndex] = i + 1;
            tilingDataHost.tensorStartOffsetList[coreIndex] = 0;
            cursorPosition = 0;
        }
    }

    if (dataCount > 0) {
        tilingDataHost.tensorEndList[coreIndex] = static_cast<uint16_t>(tilingDataHost.tensorCount - 1);
        tilingDataHost
            .tensorEndOffsetList[coreIndex] = tilingDataHost.tensorDataCountList[tilingDataHost.tensorCount - 1] - 1;
    }
}

static ge::graphStatus ForeachAddcdivScalarTilingFunc(gert::TilingContext* context)
{
    int64_t coreNum = 0;
    int64_t ubSize = 0;
    OP_CHECK_IF(GetPlatformInfoFallback(context, coreNum, ubSize) != ge::GRAPH_SUCCESS,
                OP_LOGE(context, "Failed to get platform info"), return ge::GRAPH_FAILED);
    OP_CHECK_IF((ubSize <= DCACHE_SIZE), OP_LOGE(context, "ubSize %ld <= DCACHE_SIZE %ld", ubSize, DCACHE_SIZE),
                return ge::GRAPH_FAILED);
    ubSize = ubSize - DCACHE_SIZE;

    auto computeNodeInfoPtr = context->GetComputeNodeInfo();
    OP_CHECK_NULL_WITH_CONTEXT(context, computeNodeInfoPtr);
    auto idxInstanceInfoPtr = computeNodeInfoPtr->GetInputInstanceInfo(INPUT_X1_IDX);
    OP_CHECK_NULL_WITH_CONTEXT(context, idxInstanceInfoPtr);
    uint64_t tensorNum = idxInstanceInfoPtr->GetInstanceNum();

    OP_CHECK_IF((static_cast<int32_t>(tensorNum) > MAX_TENSOR_NUM_FOREACH_ADDCDIV_SCALAR),
                OP_LOGE(context, "tensorNum %lu exceeds MAX_TENSOR_NUM_FOREACH_ADDCDIV_SCALAR %d", tensorNum,
                        MAX_TENSOR_NUM_FOREACH_ADDCDIV_SCALAR),
                return ge::GRAPH_FAILED);

    int64_t totalDataCount = 0;
    ge::DataType dataType = ge::DT_FLOAT;
    ForeachAddcdivScalarTilingDataHost tilingDataHost = {};

    tilingDataHost.tensorCount = static_cast<int32_t>(tensorNum);

    for (uint64_t i = 0; i < tensorNum; i++) {
        auto idxTensorShapePtr = context->GetDynamicInputShape(INPUT_X1_IDX, i);
        OP_CHECK_NULL_WITH_CONTEXT(context, idxTensorShapePtr);
        auto idxTensorShape = idxTensorShapePtr->GetStorageShape();
        int64_t numel = idxTensorShape.GetShapeSize();
        tilingDataHost.tensorDataCountList[i] = numel;
        totalDataCount += numel;

        if (i == 0) {
            auto idxTensorDtypePtr = context->GetDynamicInputDesc(INPUT_X1_IDX, i);
            OP_CHECK_NULL_WITH_CONTEXT(context, idxTensorDtypePtr);
            dataType = idxTensorDtypePtr->GetDataType();
        }
    }

    tilingDataHost.totalDataCount = totalDataCount;

    int64_t dataTypeSize = 0;
    switch (dataType) {
        case ge::DT_FLOAT16:
            dataTypeSize = 2;
            break;
        case ge::DT_FLOAT:
            dataTypeSize = 4;
            break;
        case ge::DT_BF16:
            dataTypeSize = 2;
            break;
        default:
            dataTypeSize = 4;
            break;
    }

    int64_t needCoreNum = (totalDataCount + SINGLE_CORE_MIN_ELEMENTS - 1) / SINGLE_CORE_MIN_ELEMENTS;
    needCoreNum = std::min(needCoreNum, coreNum);
    needCoreNum = std::max(needCoreNum, static_cast<int64_t>(1));

    if (totalDataCount == 0) {
        needCoreNum = 0;
    }

    tilingDataHost.needCoreNum = static_cast<int32_t>(needCoreNum);

    if (needCoreNum > 0) {
        AssignDataToEachCore(tilingDataHost, needCoreNum, dataTypeSize);
    }

    ForeachAddcdivScalarTilingDataHost* tilingData = context->GetTilingData<ForeachAddcdivScalarTilingDataHost>();
    *tilingData = tilingDataHost;

    context->SetBlockDim(needCoreNum);
    context->SetTilingKey(GetTilingKeyByDtype(dataType));

    auto res = context->SetLocalMemorySize(static_cast<uint32_t>(ubSize));
    OP_CHECK_IF((res != ge::GRAPH_SUCCESS), OP_LOGE(context, "SetLocalMemorySize ubSize=%ld failed", ubSize),
                return ge::GRAPH_FAILED);

    size_t* currentWorkspace = context->GetWorkspaceSizes(1);
    currentWorkspace[0] = 0;

    return ge::GRAPH_SUCCESS;
}

static ge::graphStatus TilingParseForForeachAddcdivScalar(gert::TilingParseContext* context)
{
    auto compileInfo = context->GetCompiledInfo<ForeachAddcdivScalarCompileInfo>();
    OP_CHECK_NULL_WITH_CONTEXT(context, compileInfo);
    auto platformInfo = context->GetPlatformInfo();
    OP_CHECK_NULL_WITH_CONTEXT(context, platformInfo);
    auto ascendcPlatform = platform_ascendc::PlatformAscendC(platformInfo);
    compileInfo->coreNum = ascendcPlatform.GetCoreNumAiv();
    OP_CHECK_IF((compileInfo->coreNum <= 0), OP_LOGE(context, "Failed to get core num."), return ge::GRAPH_FAILED);
    uint64_t ubSize;
    ascendcPlatform.GetCoreMemSize(platform_ascendc::CoreMemType::UB, ubSize);
    compileInfo->ubSize = static_cast<int64_t>(ubSize);
    OP_CHECK_IF((compileInfo->ubSize <= 0), OP_LOGE(context, "Failed to get ub size."), return ge::GRAPH_FAILED);
    return ge::GRAPH_SUCCESS;
}

IMPL_OP_OPTILING(ForeachAddcdivScalar)
    .Tiling(ForeachAddcdivScalarTilingFunc)
    .TilingParse<ForeachAddcdivScalarCompileInfo>(TilingParseForForeachAddcdivScalar);

} // namespace optiling
