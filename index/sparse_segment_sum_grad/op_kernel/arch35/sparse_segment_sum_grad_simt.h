/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file sparse_segment_sum_grad_simt.h
 * \brief SIMT kernel implementation for SparseSegmentSumGrad
 */

/* Generated By CANNBot */

#ifndef SPARSE_SEGMENT_SUM_GRAD_SIMT_H
#define SPARSE_SEGMENT_SUM_GRAD_SIMT_H

#include "kernel_operator.h"
#include "kernel_tiling/kernel_tiling.h"
#include "simt_api/common_functions.h"
#include "simt_api/device_sync_functions.h"
#include "simt_api/device_warp_functions.h"
#include "simt_api/asc_fp16.h"
#include "simt_api/asc_bf16.h"
#include "sparse_segment_sum_grad_tiling_data.h"
#include "sparse_segment_sum_grad_tiling_key.h"

namespace NsSparseSegmentSumGrad {

using namespace AscendC;

template <typename IDX_T>
static constexpr uint32_t THREAD_NUM = (sizeof(IDX_T) == 4) ? 1024 : 512;

constexpr uint64_t DOUBLE_ABS_MASK = 0x7FFFFFFFFFFFFFFFULL;
constexpr uint32_t DOUBLE_EXP_SHIFT = 52;
constexpr uint32_t DOUBLE_EXP_MASK = 0x7FFU;
constexpr uint64_t DOUBLE_MAN_MASK = 0x000FFFFFFFFFFFFFULL;
constexpr uint64_t DOUBLE_IMPLICIT_BIT = 0x0010000000000000ULL;
constexpr uint32_t DOUBLE_EXP_WIDTH = 11;
constexpr uint64_t DOUBLE_QUIET_NAN = 0x7FF8000000000000ULL;
constexpr uint64_t DOUBLE_INF_PATTERN = 0x7FF0000000000000ULL;
constexpr int32_t DOUBLE_DENORM_CUTOFF = -52;
constexpr uint32_t DOUBLE_SIGN_BIT_SHIFT = 63;
constexpr uint32_t LZ_STEP_32 = 32;
constexpr uint32_t LZ_STEP_16 = 16;
constexpr uint32_t LZ_STEP_8 = 8;
constexpr uint32_t LZ_STEP_4 = 4;
constexpr uint32_t LZ_STEP_2 = 2;
constexpr uint64_t LZ_MASK_32 = 0xFFFFFFFF00000000ULL;
constexpr uint64_t LZ_MASK_16 = 0xFFFF000000000000ULL;
constexpr uint64_t LZ_MASK_8 = 0xFF00000000000000ULL;
constexpr uint64_t LZ_MASK_4 = 0xF000000000000000ULL;
constexpr uint64_t LZ_MASK_2 = 0xC000000000000000ULL;
constexpr uint64_t LZ_MASK_1 = 0x8000000000000000ULL;
constexpr uint32_t SHIFT_LIMIT_64 = 64;
constexpr uint32_t DOUBLE_TYPE_SIZE = 8;

// ascend950 aicore forbids double arithmetic; implement addition via uint64_t bit manipulation
// IEEE 754 double addition using only integer operations
__simt_callee__ inline uint64_t DoubleAddBits(uint64_t a, uint64_t b)
{
    uint64_t aAbs = a & DOUBLE_ABS_MASK;
    uint64_t bAbs = b & DOUBLE_ABS_MASK;
    if (aAbs == 0)
        return b;
    if (bAbs == 0)
        return a;

    uint32_t aSign = (uint32_t)(a >> DOUBLE_SIGN_BIT_SHIFT);
    uint32_t aExp = (uint32_t)((a >> DOUBLE_EXP_SHIFT) & DOUBLE_EXP_MASK);
    uint64_t aMan = a & DOUBLE_MAN_MASK;

    uint32_t bSign = (uint32_t)(b >> DOUBLE_SIGN_BIT_SHIFT);
    uint32_t bExp = (uint32_t)((b >> DOUBLE_EXP_SHIFT) & DOUBLE_EXP_MASK);
    uint64_t bMan = b & DOUBLE_MAN_MASK;

    // NaN/Inf handling per IEEE 754
    static constexpr uint64_t QUIET_NAN = DOUBLE_QUIET_NAN;
    bool aIsNan = (aExp == DOUBLE_EXP_MASK) && (aMan != 0);
    bool bIsNan = (bExp == DOUBLE_EXP_MASK) && (bMan != 0);
    bool aIsInf = (aExp == DOUBLE_EXP_MASK) && (aMan == 0);
    bool bIsInf = (bExp == DOUBLE_EXP_MASK) && (bMan == 0);
    // NaN + anything = NaN
    if (aIsNan || bIsNan)
        return QUIET_NAN;
    // Inf + (-Inf) = NaN, (-Inf) + Inf = NaN
    if (aIsInf && bIsInf && (aSign != bSign))
        return QUIET_NAN;
    // Inf + Inf (same sign) = Inf; Inf + finite = Inf
    if (aIsInf)
        return a;
    if (bIsInf)
        return b;

    // Add implicit leading 1
    if (aExp != 0)
        aMan |= DOUBLE_IMPLICIT_BIT;
    else {
        aExp = 1;
    } // denormal
    if (bExp != 0)
        bMan |= DOUBLE_IMPLICIT_BIT;
    else {
        bExp = 1;
    } // denormal

    // Shift mantissas left by 11 to get 64-bit working space (bit 63 = implicit 1)
    aMan <<= DOUBLE_EXP_WIDTH;
    bMan <<= DOUBLE_EXP_WIDTH;

    uint32_t rExp;
    uint64_t rMan;
    uint32_t rSign;

    if (aExp >= bExp) {
        rExp = aExp;
        uint32_t shift = aExp - bExp;
        if (shift < SHIFT_LIMIT_64)
            bMan >>= shift;
        else
            bMan = 0;
    } else {
        rExp = bExp;
        uint32_t shift = bExp - aExp;
        if (shift < SHIFT_LIMIT_64)
            aMan >>= shift;
        else
            aMan = 0;
    }

    if (aSign == bSign) {
        rSign = aSign;
        rMan = aMan + bMan;
        if (rMan < aMan) {
            rMan = (rMan >> 1) | DOUBLE_IMPLICIT_BIT << DOUBLE_EXP_WIDTH;
            rExp += 1;
        }
    } else {
        if (aMan >= bMan) {
            rSign = aSign;
            rMan = aMan - bMan;
        } else {
            rSign = bSign;
            rMan = bMan - aMan;
        }
    }

    if (rMan == 0)
        return 0;

    // Normalize: find highest set bit
    uint32_t lz = 0;
    uint64_t tmp = rMan;
    if ((tmp & LZ_MASK_32) == 0) {
        lz += LZ_STEP_32;
        tmp <<= LZ_STEP_32;
    }
    if ((tmp & LZ_MASK_16) == 0) {
        lz += LZ_STEP_16;
        tmp <<= LZ_STEP_16;
    }
    if ((tmp & LZ_MASK_8) == 0) {
        lz += LZ_STEP_8;
        tmp <<= LZ_STEP_8;
    }
    if ((tmp & LZ_MASK_4) == 0) {
        lz += LZ_STEP_4;
        tmp <<= LZ_STEP_4;
    }
    if ((tmp & LZ_MASK_2) == 0) {
        lz += LZ_STEP_2;
        tmp <<= LZ_STEP_2;
    }
    if ((tmp & LZ_MASK_1) == 0) {
        lz += 1;
    }

    rMan <<= lz;
    int32_t expAdj = (int32_t)rExp - (int32_t)lz;
    if (expAdj <= 0) {
        // Denormal or zero
        if (expAdj < DOUBLE_DENORM_CUTOFF)
            return ((uint64_t)rSign << DOUBLE_SIGN_BIT_SHIFT);
        rMan >>= (1 - expAdj);
        rExp = 0;
    } else {
        rExp = (uint32_t)expAdj;
        if (rExp >= DOUBLE_EXP_MASK)
            return ((uint64_t)rSign << DOUBLE_SIGN_BIT_SHIFT) | DOUBLE_INF_PATTERN; // Inf
    }

    // Extract mantissa: bit 63 is the implicit 1, bits 62..12 are the 52-bit mantissa
    uint64_t rManField = (rMan >> DOUBLE_EXP_WIDTH) & DOUBLE_MAN_MASK;
    if (rExp != 0)
        rManField &= DOUBLE_MAN_MASK; // remove implicit 1

    return ((uint64_t)rSign << DOUBLE_SIGN_BIT_SHIFT) | ((uint64_t)rExp << DOUBLE_EXP_SHIFT) | rManField;
}

// ===== SIMT VF Kernel: Phase 1 - Zero Initialize =====
template <typename T, typename IDX_T>
__simt_vf__ __aicore__ __launch_bounds__(THREAD_NUM<IDX_T>) inline void OpSparseSegmentSumGradZeroInit(
    IDX_T totalOutputElements, __gm__ T* output)
{
    for (IDX_T idx = static_cast<IDX_T>(blockIdx.x) * static_cast<IDX_T>(blockDim.x) + static_cast<IDX_T>(threadIdx.x);
         idx < totalOutputElements; idx += static_cast<IDX_T>(blockDim.x) * static_cast<IDX_T>(gridDim.x)) {
        output[idx] = static_cast<T>(0);
    }
}

template <typename T, typename IndexT, typename SegIdT, typename IDX_T>
__simt_vf__ __aicore__ __launch_bounds__(THREAD_NUM<IDX_T>) inline void OpSparseSegmentSumGradGatherAdd(
    IDX_T n, IDX_T innerSize, IDX_T totalOutputElements, __gm__ T* grad, __gm__ IndexT* indices,
    __gm__ SegIdT* segmentIds, __gm__ T* output)
{
    for (IDX_T elemIdx =
             static_cast<IDX_T>(blockIdx.x) * static_cast<IDX_T>(blockDim.x) + static_cast<IDX_T>(threadIdx.x);
         elemIdx < totalOutputElements; elemIdx += static_cast<IDX_T>(blockDim.x) * static_cast<IDX_T>(gridDim.x)) {
        IDX_T row = elemIdx / innerSize;
        IDX_T k = elemIdx % innerSize;
        if constexpr (sizeof(T) == DOUBLE_TYPE_SIZE) {
            // double: aicore forbids double arithmetic in VF; use bit manipulation
            uint64_t sumBits = 0;
            __gm__ uint64_t* gradAsUll = (__gm__ uint64_t*)grad;
            for (IDX_T j = 0; j < n; ++j) {
                if (static_cast<IDX_T>(indices[j]) == row) {
                    int64_t gradIdx = static_cast<int64_t>(segmentIds[j]) * static_cast<int64_t>(innerSize) +
                                      static_cast<int64_t>(k);
                    uint64_t valBits = gradAsUll[gradIdx];
                    sumBits = DoubleAddBits(sumBits, valBits);
                }
            }
            __gm__ uint64_t* outputAsUll = (__gm__ uint64_t*)output;
            outputAsUll[elemIdx] = sumBits;
        } else {
            // fp16/bf16/fp32: accumulate in float for better precision
            float sum = 0.0f;
            for (IDX_T j = 0; j < n; ++j) {
                if (static_cast<IDX_T>(indices[j]) == row) {
                    int64_t gradIdx = static_cast<int64_t>(segmentIds[j]) * static_cast<int64_t>(innerSize) +
                                      static_cast<int64_t>(k);
                    sum += static_cast<float>(grad[gradIdx]);
                }
            }
            output[elemIdx] = static_cast<T>(sum);
        }
    }
}

// ===== Process function =====
template <typename T, typename IndexT, typename SegIdT>
__aicore__ inline void Process(GM_ADDR grad, GM_ADDR indices, GM_ADDR segmentIds, GM_ADDR outputDim0, GM_ADDR output,
                               const SparseSegmentSumGradTilingData* tilingData)
{
    __gm__ T* gradGm = (__gm__ T*)grad;
    __gm__ IndexT* indicesGm = (__gm__ IndexT*)indices;
    __gm__ SegIdT* segIdsGm = (__gm__ SegIdT*)segmentIds;
    __gm__ T* outputGm = (__gm__ T*)output;

    int64_t totalOut = tilingData->totalOutputElements;
    int64_t n = tilingData->N;
    int64_t innerSz = tilingData->innerSize;
    int64_t outDim0 = tilingData->outputDim0;

    bool use32 = (totalOut <= static_cast<int64_t>(INT32_MAX)) && (n <= static_cast<int64_t>(INT32_MAX)) &&
                 (innerSz <= static_cast<int64_t>(INT32_MAX)) && (outDim0 <= static_cast<int64_t>(INT32_MAX));

    // Phase 1: Zero-initialize output (always runs, even when N=0)
    if (use32) {
        asc_vf_call<OpSparseSegmentSumGradZeroInit<T, int32_t>>(dim3(THREAD_NUM<int32_t>),
                                                                static_cast<int32_t>(totalOut), outputGm);
    } else {
        asc_vf_call<OpSparseSegmentSumGradZeroInit<T, int64_t>>(dim3(THREAD_NUM<int64_t>), totalOut, outputGm);
    }

    // N=0 boundary: skip Phase 2 entirely when indices/segment_ids are empty
    // Output is already zero-initialized by Phase 1
    if (n == 0) {
        return;
    }

    SyncAll();

    // Phase 2: Deterministic gather-add (no atomics)
    if (use32) {
        asc_vf_call<OpSparseSegmentSumGradGatherAdd<T, IndexT, SegIdT, int32_t>>(
            dim3(THREAD_NUM<int32_t>), static_cast<int32_t>(n), static_cast<int32_t>(innerSz),
            static_cast<int32_t>(totalOut), gradGm, indicesGm, segIdsGm, outputGm);
    } else {
        asc_vf_call<OpSparseSegmentSumGradGatherAdd<T, IndexT, SegIdT, int64_t>>(
            dim3(THREAD_NUM<int64_t>), n, innerSz, totalOut, gradGm, indicesGm, segIdsGm, outputGm);
    }
}

} // namespace NsSparseSegmentSumGrad

#endif // SPARSE_SEGMENT_SUM_GRAD_SIMT_H
