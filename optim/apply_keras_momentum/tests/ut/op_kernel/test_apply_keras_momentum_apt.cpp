/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

#include <cstdint>
#include <cstring>
#include <cmath>
#include "gtest/gtest.h"

#ifdef __CCE_KT_TEST__
#include "tikicpulib.h"
#endif

#include "../../../op_kernel/arch35/apply_keras_momentum_tiling_data.h"

using namespace std;

extern "C" __global__ __aicore__ void apply_keras_momentum(GM_ADDR var, GM_ADDR accum, GM_ADDR lr, GM_ADDR grad,
                                                           GM_ADDR momentum, GM_ADDR var_out, GM_ADDR workspace,
                                                           GM_ADDR tiling);

class ApplyKerasMomentumKernelTest : public testing::Test {
protected:
    static void SetUpTestCase() { cout << "ApplyKerasMomentumKernelTest SetUp" << endl; }
    static void TearDownTestCase() { cout << "ApplyKerasMomentumKernelTest TearDown" << endl; }
};

TEST_F(ApplyKerasMomentumKernelTest, test_fp32_basic)
{
    constexpr size_t numElements = 256;
    size_t dataSize = numElements * sizeof(float);
    size_t scalarSize = sizeof(float);
    size_t tilingSize = sizeof(ApplyKerasMomentumTilingData);
    uint32_t blockDim = 1;

    uint8_t* var = (uint8_t*)AscendC::GmAlloc(dataSize);
    ASSERT_NE(var, nullptr) << "GmAlloc var failed";
    uint8_t* accum = (uint8_t*)AscendC::GmAlloc(dataSize);
    ASSERT_NE(accum, nullptr) << "GmAlloc accum failed";
    uint8_t* lr = (uint8_t*)AscendC::GmAlloc(scalarSize);
    ASSERT_NE(lr, nullptr) << "GmAlloc lr failed";
    uint8_t* grad = (uint8_t*)AscendC::GmAlloc(dataSize);
    ASSERT_NE(grad, nullptr) << "GmAlloc grad failed";
    uint8_t* momentum = (uint8_t*)AscendC::GmAlloc(scalarSize);
    ASSERT_NE(momentum, nullptr) << "GmAlloc momentum failed";
    uint8_t* var_out = (uint8_t*)AscendC::GmAlloc(dataSize);
    ASSERT_NE(var_out, nullptr) << "GmAlloc var_out failed";
    uint8_t* workspace = (uint8_t*)AscendC::GmAlloc(1024 * 1024);
    ASSERT_NE(workspace, nullptr) << "GmAlloc workspace failed";
    uint8_t* tiling = (uint8_t*)AscendC::GmAlloc(tilingSize);
    ASSERT_NE(tiling, nullptr) << "GmAlloc tiling failed";

    auto* varF = reinterpret_cast<float*>(var);
    auto* accumF = reinterpret_cast<float*>(accum);
    auto* gradF = reinterpret_cast<float*>(grad);
    for (size_t i = 0; i < numElements; ++i) {
        varF[i] = 1.0f;
        accumF[i] = 0.5f;
        gradF[i] = 0.1f;
    }
    *reinterpret_cast<float*>(lr) = 0.01f;
    *reinterpret_cast<float*>(momentum) = 0.9f;

    auto* tilingData = reinterpret_cast<ApplyKerasMomentumTilingData*>(tiling);
    memset(tilingData, 0, tilingSize);
    tilingData->totalElements = numElements;
    tilingData->tileLength = numElements;
    tilingData->blockDim = 1;
    tilingData->blockFormer = numElements;
    tilingData->blockNum = 1;
    tilingData->ubFormer = numElements;
    tilingData->ubLoopOfFormerBlock = 1;
    tilingData->ubTailOfFormerBlock = numElements;
    tilingData->ubLoopOfTailBlock = 1;
    tilingData->ubTailOfTailBlock = numElements;
    tilingData->dtypeSize = 4;
    tilingData->useNesterov = 0;

    AscendC::SetKernelMode(KernelMode::AIV_MODE);
    ICPU_RUN_KF(apply_keras_momentum, blockDim, var, accum, lr, grad, momentum, var_out, workspace, tiling);

    // accum_new = 0.9 * 0.5 - 0.01 * 0.1 = 0.449
    // var_out = 1.0 + 0.449 = 1.449
    auto* voF = reinterpret_cast<float*>(var_out);
    for (size_t i = 0; i < numElements; ++i) {
        ASSERT_NEAR(accumF[i], 0.449f, 1e-4f) << "accum mismatch at [" << i << "]";
        ASSERT_NEAR(voF[i], 1.449f, 1e-4f) << "var_out mismatch at [" << i << "]";
    }

    AscendC::GmFree(var);
    AscendC::GmFree(accum);
    AscendC::GmFree(lr);
    AscendC::GmFree(grad);
    AscendC::GmFree(momentum);
    AscendC::GmFree(var_out);
    AscendC::GmFree(workspace);
    AscendC::GmFree(tiling);
}

TEST_F(ApplyKerasMomentumKernelTest, test_fp32_nesterov)
{
    constexpr size_t numElements = 256;
    size_t dataSize = numElements * sizeof(float);
    size_t scalarSize = sizeof(float);
    size_t tilingSize = sizeof(ApplyKerasMomentumTilingData);
    uint32_t blockDim = 1;

    uint8_t* var = (uint8_t*)AscendC::GmAlloc(dataSize);
    ASSERT_NE(var, nullptr) << "GmAlloc var failed";
    uint8_t* accum = (uint8_t*)AscendC::GmAlloc(dataSize);
    ASSERT_NE(accum, nullptr) << "GmAlloc accum failed";
    uint8_t* lr = (uint8_t*)AscendC::GmAlloc(scalarSize);
    ASSERT_NE(lr, nullptr) << "GmAlloc lr failed";
    uint8_t* grad = (uint8_t*)AscendC::GmAlloc(dataSize);
    ASSERT_NE(grad, nullptr) << "GmAlloc grad failed";
    uint8_t* momentum = (uint8_t*)AscendC::GmAlloc(scalarSize);
    ASSERT_NE(momentum, nullptr) << "GmAlloc momentum failed";
    uint8_t* var_out = (uint8_t*)AscendC::GmAlloc(dataSize);
    ASSERT_NE(var_out, nullptr) << "GmAlloc var_out failed";
    uint8_t* workspace = (uint8_t*)AscendC::GmAlloc(1024 * 1024);
    ASSERT_NE(workspace, nullptr) << "GmAlloc workspace failed";
    uint8_t* tiling = (uint8_t*)AscendC::GmAlloc(tilingSize);
    ASSERT_NE(tiling, nullptr) << "GmAlloc tiling failed";

    auto* varF = reinterpret_cast<float*>(var);
    auto* accumF = reinterpret_cast<float*>(accum);
    auto* gradF = reinterpret_cast<float*>(grad);
    for (size_t i = 0; i < numElements; ++i) {
        varF[i] = 1.0f;
        accumF[i] = 0.5f;
        gradF[i] = 0.1f;
    }
    *reinterpret_cast<float*>(lr) = 0.01f;
    *reinterpret_cast<float*>(momentum) = 0.9f;

    auto* tilingData = reinterpret_cast<ApplyKerasMomentumTilingData*>(tiling);
    memset(tilingData, 0, tilingSize);
    tilingData->totalElements = numElements;
    tilingData->tileLength = numElements;
    tilingData->blockDim = 1;
    tilingData->blockFormer = numElements;
    tilingData->blockNum = 1;
    tilingData->ubFormer = numElements;
    tilingData->ubLoopOfFormerBlock = 1;
    tilingData->ubTailOfFormerBlock = numElements;
    tilingData->ubLoopOfTailBlock = 1;
    tilingData->ubTailOfTailBlock = numElements;
    tilingData->dtypeSize = 4;
    tilingData->useNesterov = 1;

    AscendC::SetKernelMode(KernelMode::AIV_MODE);
    ICPU_RUN_KF(apply_keras_momentum, blockDim, var, accum, lr, grad, momentum, var_out, workspace, tiling);

    // accum_new = 0.9 * 0.5 - 0.01 * 0.1 = 0.449
    // var_out = 1.0 + 0.9 * 0.449 - 0.01 * 0.1 = 1.4031
    auto* voF = reinterpret_cast<float*>(var_out);
    for (size_t i = 0; i < numElements; ++i) {
        ASSERT_NEAR(accumF[i], 0.449f, 1e-4f) << "accum mismatch at [" << i << "]";
        ASSERT_NEAR(voF[i], 1.4031f, 1e-3f) << "var_out mismatch at [" << i << "]";
    }

    AscendC::GmFree(var);
    AscendC::GmFree(accum);
    AscendC::GmFree(lr);
    AscendC::GmFree(grad);
    AscendC::GmFree(momentum);
    AscendC::GmFree(var_out);
    AscendC::GmFree(workspace);
    AscendC::GmFree(tiling);
}
