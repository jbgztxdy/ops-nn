#!/usr/bin/env python3
# -*- coding: utf-8 -*-
# Copyright (c) 2026 Huawei Technologies Co., Ltd.
# This program is free software, you can redistribute it and/or modify it under the terms and conditions of
# CANN Open Software License Agreement Version 2.0 (the "License").
# Please refer to the License for details. You may not use this file except in compliance with the License.
# THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
# INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
# See LICENSE in the root of the software repository for the full text of the License.
# ----------------------------------------------------------------------------

# Generated By CANNBot
import numpy as np


__golden__ = {
    "kernel": {
        "apply_keras_momentum": "apply_keras_momentum_golden"
    }
}


def apply_keras_momentum_golden(var, accum, grad, lr, momentum, use_nesterov, **kwargs):
    '''
    Golden function for apply_keras_momentum.
    Implements the Keras momentum update rule:

        accum_new = momentum * accum - lr * grad
        if use_nesterov:
            var_new = var + momentum * accum_new - lr * grad
        else:
            var_new = var + accum_new

    Inputs follow the operator definition order:
        var, accum, grad, lr, momentum, use_nesterov  (numpy.ndarray each)

    Returns:
        [var_out, accum_out] -- same dtype as inputs.
    '''
    input_dtype = var.dtype
    if input_dtype.name in ("float16", "bfloat16"):
        var = var.astype("float32")
        accum = accum.astype("float32")
        grad = grad.astype("float32")
        lr = np.asarray(lr, dtype="float32")
        momentum = np.asarray(momentum, dtype="float32")
        use_nesterov = np.asarray(use_nesterov, dtype="float32")

    lr_val = float(np.asarray(lr).flat[0])
    mom_val = float(np.asarray(momentum).flat[0])
    nest_val = float(np.asarray(use_nesterov).flat[0])

    accum_new = mom_val * accum - lr_val * grad

    if nest_val != 0.0:
        var_new = var + mom_val * accum_new - lr_val * grad
    else:
        var_new = var + accum_new

    return [var_new.astype(input_dtype, copy=False),
            accum_new.astype(input_dtype, copy=False)]
