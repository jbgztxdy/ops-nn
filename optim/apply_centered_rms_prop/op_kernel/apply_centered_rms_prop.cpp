/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file apply_centered_rms_prop_arch35.cpp
 * \brief ApplyCenteredRMSProp kernel entry point (arch35 / Ascend950)
 *
 * Dtype dispatch: the framework compiles one kernel binary per supported
 * dtype and injects the C++ type via the DTYPE_VAR macro. No tilingKey is
 * needed for dtype selection.
 *
 * Kernel parameters (9 tensor inputs + single output var + workspace + tiling):
 *   var, mg, ms, mom: ND state tensors (in-place updated; mg/ms/mom write back
 *                     to their own input GM, matching canndev 1-output IR)
 *   lr, rho, momentum, epsilon: 0-D scalar tensors
 *   grad: ND gradient tensor
 *   varOut: sole output; aliases var (in-place), so var GM is reused directly
 */

#include "arch35/apply_centered_rms_prop.h"

#ifdef __CCE_KT_TEST__
// UT mode: template version for multi-dtype testing in a single binary.
template <typename D_T>
__global__ __aicore__ void apply_centered_rms_prop(
    GM_ADDR var, GM_ADDR mg, GM_ADDR ms, GM_ADDR mom,
    GM_ADDR lr, GM_ADDR rho, GM_ADDR momentum, GM_ADDR epsilon,
    GM_ADDR grad,
    GM_ADDR varOut,
    GM_ADDR workspace, GM_ADDR tiling)
{
    REGISTER_TILING_DEFAULT(ApplyCenteredRMSPropTilingData);
    GET_TILING_DATA_WITH_STRUCT(ApplyCenteredRMSPropTilingData, tilingData, tiling);

    NsApplyCenteredRMSProp::ApplyCenteredRMSProp<D_T> op;
    op.Init(var, mg, ms, mom, lr, rho, momentum, epsilon, grad, &tilingData);
    op.Process();
}
#else
// Real build: DTYPE_VAR is injected by the framework per dtype compilation.
extern "C" __global__ __aicore__ void apply_centered_rms_prop(
    GM_ADDR var, GM_ADDR mg, GM_ADDR ms, GM_ADDR mom,
    GM_ADDR lr, GM_ADDR rho, GM_ADDR momentum, GM_ADDR epsilon,
    GM_ADDR grad,
    GM_ADDR varOut,
    GM_ADDR workspace, GM_ADDR tiling)
{
    REGISTER_TILING_DEFAULT(ApplyCenteredRMSPropTilingData);
    GET_TILING_DATA_WITH_STRUCT(ApplyCenteredRMSPropTilingData, tilingData, tiling);

    NsApplyCenteredRMSProp::ApplyCenteredRMSProp<DTYPE_VAR> op;
    op.Init(var, mg, ms, mom, lr, rho, momentum, epsilon, grad, &tilingData);
    op.Process();
}
#endif
