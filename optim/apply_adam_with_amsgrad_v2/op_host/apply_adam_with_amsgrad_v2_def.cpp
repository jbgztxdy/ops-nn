/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file apply_adam_with_amsgrad_v2_def.cpp
 * \brief ApplyAdamWithAmsgradV2 算子定义（OpDef 注册）
 *
 * 输入输出契约：
 *   11 个输入（var/m/v/vhat 为 state；6 标量；grad）+ 4 个输出（inplace var/m/v/vhat）
 *   + 1 属性（use_locking，语义占位，不参与计算）。
 *
 * var/m/v/vhat/grad 与 4 输出仅支持 fp32；6 标量恒 fp32（host const）
 *   + ValueDepend(REQUIRED, TILING) 让 host Tiling 阶段可读取标量值。
 */
#include "register/op_def_registry.h"

namespace ops {

class ApplyAdamWithAmsgradV2 : public OpDef {
public:
    explicit ApplyAdamWithAmsgradV2(const char* name) : OpDef(name)
    {
        // ---- 状态张量输入（var/m/v/vhat）+ grad，dtype 随 var ----
        this->Input("var")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT})
            .Format({ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND})
            .AutoContiguous();
        this->Input("m")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT})
            .Format({ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND})
            .AutoContiguous();
        this->Input("v")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT})
            .Format({ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND})
            .AutoContiguous();
        this->Input("vhat")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT})
            .Format({ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND})
            .AutoContiguous();
        // ---- 标量 (1,) 输入：ValueDepend(REQUIRED, TILING) 让 host 直读 const ----
        this->Input("beta1_power")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT})
            .Format({ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND})
            .ValueDepend(REQUIRED, DependScope::TILING)
            .AutoContiguous();
        this->Input("beta2_power")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT})
            .Format({ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND})
            .ValueDepend(REQUIRED, DependScope::TILING)
            .AutoContiguous();
        this->Input("lr")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT})
            .Format({ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND})
            .ValueDepend(REQUIRED, DependScope::TILING)
            .AutoContiguous();
        this->Input("beta1")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT})
            .Format({ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND})
            .ValueDepend(REQUIRED, DependScope::TILING)
            .AutoContiguous();
        this->Input("beta2")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT})
            .Format({ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND})
            .ValueDepend(REQUIRED, DependScope::TILING)
            .AutoContiguous();
        this->Input("epsilon")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT})
            .Format({ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND})
            .ValueDepend(REQUIRED, DependScope::TILING)
            .AutoContiguous();
        this->Input("grad")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT})
            .Format({ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND})
            .AutoContiguous();

        // ---- 输出（4 个，与对应输入 inplace 同名，与 proto REG_OP 保持一致；dtype 跟随 var） ----
        this->Output("var")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT})
            .Format({ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND})
            .AutoContiguous();
        this->Output("m")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT})
            .Format({ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND})
            .AutoContiguous();
        this->Output("v")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT})
            .Format({ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND})
            .AutoContiguous();
        this->Output("vhat")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT})
            .Format({ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND})
            .AutoContiguous();

        // ---- 属性（use_locking，语义占位，与 proto REG_OP 一致；不参与计算） ----
        this->Attr("use_locking").AttrType(OPTIONAL).Bool(false);

        // ---- AICore 配置：仅 ascend950（arch35 / DAV_3510） ----
        OpAICoreConfig aiCoreConfig;
        aiCoreConfig.DynamicCompileStaticFlag(true)
            .DynamicFormatFlag(false)
            .DynamicRankSupportFlag(true)
            .DynamicShapeSupportFlag(true)
            .NeedCheckSupportFlag(false)
            .PrecisionReduceFlag(true)
            .ExtendCfgInfo("opFile.value", "apply_adam_with_amsgrad_v2");
        this->AICore().AddConfig("ascend950", aiCoreConfig);
    }
};

OP_ADD(ApplyAdamWithAmsgradV2);

} // namespace ops
