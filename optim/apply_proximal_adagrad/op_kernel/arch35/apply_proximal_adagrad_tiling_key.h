/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/**
 * NOTE: Portions of this code were AI-generated and have been
 * technically reviewed for functional accuracy and security
 * Generated By CANNBot
 */

/*!
 * \file apply_proximal_adagrad_tiling_key.h
 * \brief ApplyProximalAdagrad TilingKey template-argument declaration (arch35).
 *
 * The kernel exposes 12 binaries via the (D_T_VAR, PAD_TAIL, HAS_L1) tuple:
 *
 *   D_T_VAR  in {C_DT_FLOAT, C_DT_FLOAT16, C_DT_BF16}
 *   PAD_TAIL in {0, 1}    (0 = aligned tail, 1 = non-aligned tail)
 *   HAS_L1   in {0, 1}    (0 = dedicated fast path without sign+soft-threshold,
 *                          1 = general path; kernel still takes a runtime
 *                          fast-path when l1Scalar_ == 0)
 *
 * - PAD_TAIL is derived in host tiling from (totalElements % ubBlockSize != 0).
 * - HAS_L1 is normally driven from host = 1 (lr/l1/l2 sit in Device GM and we
 *   cannot synchronously inspect them at tiling time).  The kernel additionally
 *   performs a cheap runtime fast-path check when l1Scalar == 0.  We still
 *   register the HAS_L1 = 0 binary so UT (and a future L0-API hint) can drive
 *   the dedicated branch.
 *
 * dtype extension (built-in transition):
 *   fp16 / bf16 inputs are cast to fp32 for intermediate compute and cast
 *   back on output. bf16 scalar loading (LoadScalar) and bf16(1.0) tail-pad
 *   construction route through the supported vector-path Cast instruction
 *   to avoid the bisheng "not support bf16 type cast" backend limitation on
 *   the scalar path; see apply_proximal_adagrad.h for details.
 *
 * Uses ASCENDC_TPL_ARGS_DECL template-argument mechanism.
 * TILING_KEY_IS macro is forbidden.
 */

#ifndef __APPLY_PROXIMAL_ADAGRAD_TILING_KEY_H__
#define __APPLY_PROXIMAL_ADAGRAD_TILING_KEY_H__

#include "ascendc/host_api/tiling/template_argument.h"

// CPU UT (`__CCE_KT_TEST__`) 走 ICPU_RUN_KF 路径, 直接以非模板 extern "C"
// 入口编译, 不参与 host tiling key 注册; 而 C_DT_FLOAT / ASCENDC_TPL_* 这
// 套宏在 UT 仅依赖的 kernel 头集合里没有定义, 因此在 UT 编译期屏蔽掉这段
// 注册可避免 "C_DT_FLOAT was not declared" 之类的硬错。
#ifndef __CCE_KT_TEST__
ASCENDC_TPL_ARGS_DECL(ApplyProximalAdagrad,
                      ASCENDC_TPL_DATATYPE_DECL(D_T_VAR, C_DT_FLOAT, C_DT_FLOAT16, C_DT_BF16, ASCENDC_TPL_INPUT(0)),
                      ASCENDC_TPL_UINT_DECL(PAD_TAIL, 8, ASCENDC_TPL_UI_LIST, 0, 1),
                      ASCENDC_TPL_UINT_DECL(HAS_L1, 8, ASCENDC_TPL_UI_LIST, 0, 1));

ASCENDC_TPL_SEL(ASCENDC_TPL_ARGS_SEL(ASCENDC_TPL_DATATYPE_SEL(D_T_VAR, C_DT_FLOAT),
                                     ASCENDC_TPL_UINT_SEL(PAD_TAIL, ASCENDC_TPL_UI_LIST, 0, 1),
                                     ASCENDC_TPL_UINT_SEL(HAS_L1, ASCENDC_TPL_UI_LIST, 0, 1)),
                ASCENDC_TPL_ARGS_SEL(ASCENDC_TPL_DATATYPE_SEL(D_T_VAR, C_DT_FLOAT16),
                                     ASCENDC_TPL_UINT_SEL(PAD_TAIL, ASCENDC_TPL_UI_LIST, 0, 1),
                                     ASCENDC_TPL_UINT_SEL(HAS_L1, ASCENDC_TPL_UI_LIST, 0, 1)),
                ASCENDC_TPL_ARGS_SEL(ASCENDC_TPL_DATATYPE_SEL(D_T_VAR, C_DT_BF16),
                                     ASCENDC_TPL_UINT_SEL(PAD_TAIL, ASCENDC_TPL_UI_LIST, 0, 1),
                                     ASCENDC_TPL_UINT_SEL(HAS_L1, ASCENDC_TPL_UI_LIST, 0, 1)), );
#endif // __CCE_KT_TEST__

#endif // __APPLY_PROXIMAL_ADAGRAD_TILING_KEY_H__
