/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 *
 * Generated By CANNBot
 */

/**
 * NOTE: Portions of this code were AI-generated and have been
 * technically reviewed for functional accuracy and security
 */
/*!
 * \file apply_adamax_infer_datatype.cpp
 * \brief ApplyAdaMax 算子输出数据类型推导实现
 *
 * 输出 dtype 推导规则：
 *   - var dtype = var dtype（单输出）
 *
 * 背景：GE IR 图模式下，GE 会在 InferShapePass 中调用 InferDtypeByRegisteredFuncOrRule，
 * 未注册 InferDataType 时会报错 "Can not find ... custom infer_datatype func"。
 *
 * 注册方式说明：CANN 9.0.0 的 register/op_impl_registry.h 没有单独的
 * IMPL_OP_INFERDATATYPE 宏，只暴露了 IMPL_OP(op_type)（基于 __COUNTER__ 生成
 * 唯一符号名、可多次使用）和专用的 IMPL_OP_INFERSHAPE / IMPL_OP_OPTILING。
 * 因此这里使用 IMPL_OP(ApplyAdaMax).InferDataType(...) 完成 datatype 推导注册，
 * 与 apply_adamax_infershape.cpp 中的 IMPL_OP_INFERSHAPE(ApplyAdaMax).InferShape(...)
 * 并列存在、互不冲突。
 */

#include "register/op_impl_registry.h"
#include "exe_graph/runtime/infer_datatype_context.h"
#include "op_common/log/log.h"

using namespace ge;

namespace ops {

// 输入索引（与 apply_adamax_def.cpp 保持一致）
static constexpr size_t IDX_VAR = 0;

// 输出索引（仅 1 个：var）
static constexpr size_t OUT_VAR = 0;

static ge::graphStatus InferDataType4ApplyAdaMax(gert::InferDataTypeContext* context)
{
    // var 输出 dtype = var 输入 dtype
    const ge::DataType varDtype = context->GetInputDataType(IDX_VAR);
    context->SetOutputDataType(OUT_VAR, varDtype);

    return ge::GRAPH_SUCCESS;
}

// 使用 IMPL_OP（通用宏）单独注册 InferDataType，与 IMPL_OP_INFERSHAPE 并存。
IMPL_OP(ApplyAdaMax).InferDataType(InferDataType4ApplyAdaMax);

} // namespace ops
