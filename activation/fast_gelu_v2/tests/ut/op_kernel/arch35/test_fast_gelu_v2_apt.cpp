/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

#include <array>
#include <vector>
#include <iostream>
#include <string>
#include <cstdint>
#include "gtest/gtest.h"
#include "tikicpulib.h"
#include "data_utils.h"
#include "kernel_ut_data_helper.h"
#include "kernel_ut_data_executor.h"

#define __FAST_GELU_V2_TILING_KEY_H__
#include "../../../../op_kernel/arch35/fast_gelu_v2_tiling_data.h"
#include "../../../../op_kernel/arch35/fast_gelu_v2.h"

using namespace std;

template <typename T>
static void fast_gelu_v2_kernel(GM_ADDR x, GM_ADDR y, GM_ADDR workspace, GM_ADDR tiling)
{
    const FastGeluV2TilingData* tilingData = reinterpret_cast<const FastGeluV2TilingData*>(tiling);
    NsFastGeluV2::FastGeluV2<T, 0> op;
    op.Init(x, y, tilingData);
    op.Process();
}

class fast_gelu_v2_test : public testing::Test {
protected:
    static void SetUpTestCase()
    {
        cout << "fast_gelu_v2_test SetUp\n" << endl;
    }
    static void TearDownTestCase()
    {
        cout << "fast_gelu_v2_test TearDown\n" << endl;
        kernel_ut::CleanGeneratedBinFiles("./fast_gelu_v2_data");
    }
};

TEST_F(fast_gelu_v2_test, test_case_fp32)
{
    size_t dataNum = 256;
    size_t xByteSize = dataNum * sizeof(float);
    size_t yByteSize = dataNum * sizeof(float);
    size_t tiling_data_size = sizeof(FastGeluV2TilingData);

    uint8_t* x = (uint8_t*)AscendC::GmAlloc(xByteSize);
    uint8_t* y = (uint8_t*)AscendC::GmAlloc(yByteSize);
    uint8_t* workspace = (uint8_t*)AscendC::GmAlloc(16 * 1024 * 1024);
    uint8_t* tiling = (uint8_t*)AscendC::GmAlloc(tiling_data_size);
    uint32_t blockDim = 1;
    kernel_ut::SetupTestEnvironment("activation/fast_gelu_v2/tests/ut/op_kernel/fast_gelu_v2_data",
                                    "fast_gelu_v2_data");
    kernel_ut::RunGenData("./fast_gelu_v2_data", {"'(256)'", "float32"});


    std::string path = kernel_ut::GetTestWorkDir();

    FastGeluV2TilingData* tilingData = reinterpret_cast<FastGeluV2TilingData*>(tiling);
    tilingData->totalNum = static_cast<int64_t>(dataNum);
    tilingData->blockFactor = static_cast<int64_t>(dataNum);
    tilingData->ubFactor = static_cast<int64_t>(dataNum);

    ReadFile(path + "/fast_gelu_v2_data/input_x.bin", xByteSize, x, xByteSize);
    auto KernelFastGeluV2 = [](GM_ADDR x, GM_ADDR y, GM_ADDR workspace, GM_ADDR tiling) {
        fast_gelu_v2_kernel<float>(x, y, workspace, tiling);
    };
    ICPU_SET_TILING_KEY(0);
    AscendC::SetKernelMode(KernelMode::AIV_MODE);
    ICPU_RUN_KF(KernelFastGeluV2, blockDim, x, y, workspace, (uint8_t*)(tilingData));
    WriteFile(path + "/fast_gelu_v2_data/output.bin", y, yByteSize);

    AscendC::GmFree(x);
    AscendC::GmFree(y);
    AscendC::GmFree(workspace);
    AscendC::GmFree(tiling);
}

TEST_F(fast_gelu_v2_test, test_case_fp16)
{
    size_t dataNum = 256;
    size_t xByteSize = dataNum * sizeof(uint16_t);
    size_t yByteSize = dataNum * sizeof(uint16_t);
    size_t tiling_data_size = sizeof(FastGeluV2TilingData);

    uint8_t* x = (uint8_t*)AscendC::GmAlloc(xByteSize);
    uint8_t* y = (uint8_t*)AscendC::GmAlloc(yByteSize);
    uint8_t* workspace = (uint8_t*)AscendC::GmAlloc(16 * 1024 * 1024);
    uint8_t* tiling = (uint8_t*)AscendC::GmAlloc(tiling_data_size);
    uint32_t blockDim = 1;
    kernel_ut::SetupTestEnvironment("activation/fast_gelu_v2/tests/ut/op_kernel/fast_gelu_v2_data",
                                    "fast_gelu_v2_data");
    kernel_ut::RunGenData("./fast_gelu_v2_data", {"'(256)'", "float16"});

    std::string path = kernel_ut::GetTestWorkDir();

    FastGeluV2TilingData* tilingData = reinterpret_cast<FastGeluV2TilingData*>(tiling);
    tilingData->totalNum = static_cast<int64_t>(dataNum);
    tilingData->blockFactor = static_cast<int64_t>(dataNum);
    tilingData->ubFactor = static_cast<int64_t>(dataNum);

    ReadFile(path + "/fast_gelu_v2_data/input_x.bin", xByteSize, x, xByteSize);
    auto KernelFastGeluV2 = [](GM_ADDR x, GM_ADDR y, GM_ADDR workspace, GM_ADDR tiling) {
        fast_gelu_v2_kernel<half>(x, y, workspace, tiling);
    };
    ICPU_SET_TILING_KEY(0);
    AscendC::SetKernelMode(KernelMode::AIV_MODE);
    ICPU_RUN_KF(KernelFastGeluV2, blockDim, x, y, workspace, (uint8_t*)(tilingData));
    WriteFile(path + "/fast_gelu_v2_data/output.bin", y, yByteSize);

    AscendC::GmFree(x);
    AscendC::GmFree(y);
    AscendC::GmFree(workspace);
    AscendC::GmFree(tiling);
}
