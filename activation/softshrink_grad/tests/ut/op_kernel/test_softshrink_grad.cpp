/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

#include <cstdint>
#include <cstring>
#include <cmath>
#include "gtest/gtest.h"

#ifdef __CCE_KT_TEST__
#include "tikicpulib.h"
#endif

#include "../../../op_kernel/arch35/softshrink_grad_tiling_data.h"

using namespace std;

extern "C" __global__ __aicore__ void softshrink_grad(GM_ADDR input_grad, GM_ADDR input_x, GM_ADDR output_y,
                                                      GM_ADDR workspace, GM_ADDR tiling);

class SoftShrinkGradKernelTest : public testing::Test {
protected:
    static void SetUpTestCase() { cout << "SoftShrinkGradKernelTest SetUp" << endl; }
    static void TearDownTestCase() { cout << "SoftShrinkGradKernelTest TearDown" << endl; }
};

TEST_F(SoftShrinkGradKernelTest, test_fp32_basic)
{
    constexpr size_t numElements = 256;
    size_t dataSize = numElements * sizeof(float);
    size_t tilingSize = sizeof(SoftShrinkGradTilingData);
    uint32_t blockDim = 1;

    uint8_t* gradBuf = (uint8_t*)AscendC::GmAlloc(dataSize);
    uint8_t* xBuf = (uint8_t*)AscendC::GmAlloc(dataSize);
    uint8_t* yBuf = (uint8_t*)AscendC::GmAlloc(dataSize);
    uint8_t* workspace = (uint8_t*)AscendC::GmAlloc(1024 * 1024);
    uint8_t* tiling = (uint8_t*)AscendC::GmAlloc(tilingSize);

    float* gradPtr = reinterpret_cast<float*>(gradBuf);
    float* xPtr = reinterpret_cast<float*>(xBuf);
    for (size_t i = 0; i < numElements; i++) {
        gradPtr[i] = static_cast<float>(i % 7) * 0.3f - 1.0f;
        xPtr[i] = static_cast<float>(i % 11) * 0.5f - 2.5f;
    }

    SoftShrinkGradTilingData* td = reinterpret_cast<SoftShrinkGradTilingData*>(tiling);
    td->dim0 = numElements;
    td->coreNum = 1;
    td->blockFormer = numElements;
    td->blockNum = 1;
    td->ubFormer = numElements;
    td->lambd = 0.5f;

    AscendC::SetKernelMode(KernelMode::AIV_MODE);
    ICPU_RUN_KF(softshrink_grad, blockDim, gradBuf, xBuf, yBuf, workspace, tiling);

    AscendC::GmFree(gradBuf);
    AscendC::GmFree(xBuf);
    AscendC::GmFree(yBuf);
    AscendC::GmFree(workspace);
    AscendC::GmFree(tiling);
}

TEST_F(SoftShrinkGradKernelTest, test_fp32_large_tiled)
{
    constexpr size_t numElements = 2048;
    size_t dataSize = numElements * sizeof(float);
    size_t tilingSize = sizeof(SoftShrinkGradTilingData);
    uint32_t blockDim = 1;

    uint8_t* gradBuf = (uint8_t*)AscendC::GmAlloc(dataSize);
    uint8_t* xBuf = (uint8_t*)AscendC::GmAlloc(dataSize);
    uint8_t* yBuf = (uint8_t*)AscendC::GmAlloc(dataSize);
    uint8_t* workspace = (uint8_t*)AscendC::GmAlloc(1024 * 1024);
    uint8_t* tiling = (uint8_t*)AscendC::GmAlloc(tilingSize);

    float* gradPtr = reinterpret_cast<float*>(gradBuf);
    float* xPtr = reinterpret_cast<float*>(xBuf);
    for (size_t i = 0; i < numElements; i++) {
        gradPtr[i] = static_cast<float>(i % 13) * 0.2f - 1.2f;
        xPtr[i] = static_cast<float>(i % 17) * 1.0f - 8.0f;
    }

    SoftShrinkGradTilingData* td = reinterpret_cast<SoftShrinkGradTilingData*>(tiling);
    td->dim0 = numElements;
    td->coreNum = 1;
    td->blockFormer = numElements;
    td->blockNum = 1;
    td->ubFormer = 512;
    td->lambd = 0.5f;

    AscendC::SetKernelMode(KernelMode::AIV_MODE);
    ICPU_RUN_KF(softshrink_grad, blockDim, gradBuf, xBuf, yBuf, workspace, tiling);

    AscendC::GmFree(gradBuf);
    AscendC::GmFree(xBuf);
    AscendC::GmFree(yBuf);
    AscendC::GmFree(workspace);
    AscendC::GmFree(tiling);
}

TEST_F(SoftShrinkGradKernelTest, test_fp32_zero_lambd)
{
    constexpr size_t numElements = 256;
    size_t dataSize = numElements * sizeof(float);
    size_t tilingSize = sizeof(SoftShrinkGradTilingData);
    uint32_t blockDim = 1;

    uint8_t* gradBuf = (uint8_t*)AscendC::GmAlloc(dataSize);
    uint8_t* xBuf = (uint8_t*)AscendC::GmAlloc(dataSize);
    uint8_t* yBuf = (uint8_t*)AscendC::GmAlloc(dataSize);
    uint8_t* workspace = (uint8_t*)AscendC::GmAlloc(1024 * 1024);
    uint8_t* tiling = (uint8_t*)AscendC::GmAlloc(tilingSize);

    float* gradPtr = reinterpret_cast<float*>(gradBuf);
    float* xPtr = reinterpret_cast<float*>(xBuf);
    for (size_t i = 0; i < numElements; i++) {
        gradPtr[i] = 1.0f;
        xPtr[i] = static_cast<float>(i % 5) * 0.1f;
    }

    SoftShrinkGradTilingData* td = reinterpret_cast<SoftShrinkGradTilingData*>(tiling);
    td->dim0 = numElements;
    td->coreNum = 1;
    td->blockFormer = numElements;
    td->blockNum = 1;
    td->ubFormer = numElements;
    td->lambd = 0.0f;

    AscendC::SetKernelMode(KernelMode::AIV_MODE);
    ICPU_RUN_KF(softshrink_grad, blockDim, gradBuf, xBuf, yBuf, workspace, tiling);

    AscendC::GmFree(gradBuf);
    AscendC::GmFree(xBuf);
    AscendC::GmFree(yBuf);
    AscendC::GmFree(workspace);
    AscendC::GmFree(tiling);
}

TEST_F(SoftShrinkGradKernelTest, test_fp32_non_aligned)
{
    constexpr size_t numElements = 17;
    constexpr size_t ubFormer = 64; // 256B / sizeof(float) = 64, must >= numElements
    size_t dataSize = ubFormer * sizeof(float);
    size_t tilingSize = sizeof(SoftShrinkGradTilingData);
    uint32_t blockDim = 1;

    uint8_t* gradBuf = (uint8_t*)AscendC::GmAlloc(dataSize);
    uint8_t* xBuf = (uint8_t*)AscendC::GmAlloc(dataSize);
    uint8_t* yBuf = (uint8_t*)AscendC::GmAlloc(dataSize);
    uint8_t* workspace = (uint8_t*)AscendC::GmAlloc(1024 * 1024);
    uint8_t* tiling = (uint8_t*)AscendC::GmAlloc(tilingSize);

    float* gradPtr = reinterpret_cast<float*>(gradBuf);
    float* xPtr = reinterpret_cast<float*>(xBuf);
    memset(gradPtr, 0, dataSize);
    memset(xPtr, 0, dataSize);
    for (size_t i = 0; i < numElements; i++) {
        gradPtr[i] = static_cast<float>(i) * 0.3f - 2.5f;
        xPtr[i] = static_cast<float>(i) * 0.6f - 5.0f;
    }

    SoftShrinkGradTilingData* td = reinterpret_cast<SoftShrinkGradTilingData*>(tiling);
    td->dim0 = numElements;
    td->coreNum = 1;
    td->blockFormer = numElements;
    td->blockNum = 1;
    td->ubFormer = ubFormer;
    td->lambd = 0.5f;

    AscendC::SetKernelMode(KernelMode::AIV_MODE);
    ICPU_RUN_KF(softshrink_grad, blockDim, gradBuf, xBuf, yBuf, workspace, tiling);

    AscendC::GmFree(gradBuf);
    AscendC::GmFree(xBuf);
    AscendC::GmFree(yBuf);
    AscendC::GmFree(workspace);
    AscendC::GmFree(tiling);
}
