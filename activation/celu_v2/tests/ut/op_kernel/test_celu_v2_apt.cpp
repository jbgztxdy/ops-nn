/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

#include <array>
#include <vector>
#include <iostream>
#include <string>
#include <cstdint>
#include "gtest/gtest.h"
#include "tikicpulib.h"
#include "data_utils.h"
#include "kernel_ut_data_helper.h"
#include "kernel_ut_data_executor.h"

#include "../../../op_kernel/celu_v2_apt.cpp"

using namespace std;

extern "C" __global__ __aicore__ void celu_v2(GM_ADDR x, GM_ADDR y, GM_ADDR workspace, GM_ADDR tiling);

class celu_v2_test : public testing::Test {
protected:
    static void SetUpTestCase() { cout << "celu_v2_test SetUp\n" << endl; }
    static void TearDownTestCase()
    {
        cout << "celu_v2_test TearDown\n" << endl;
        kernel_ut::CleanGeneratedBinFiles("./celu_v2_data");
    }
};

TEST_F(celu_v2_test, test_case_fp32_alpha1)
{
    size_t dataNum = 256;
    float alpha = 1.0f;
    float invAlpha = 1.0f / alpha;
    size_t xByteSize = dataNum * sizeof(float);
    size_t yByteSize = dataNum * sizeof(float);
    size_t tiling_data_size = sizeof(CeluV2TilingData);

    uint8_t* x = (uint8_t*)AscendC::GmAlloc(xByteSize);
    uint8_t* y = (uint8_t*)AscendC::GmAlloc(yByteSize);
    uint8_t* workspace = (uint8_t*)AscendC::GmAlloc(16 * 1024 * 1024);
    uint8_t* tiling = (uint8_t*)AscendC::GmAlloc(tiling_data_size);
    uint32_t blockDim = 1;
    kernel_ut::SetupTestEnvironment("activation/celu_v2/tests/ut/op_kernel/celu_v2_data", "celu_v2_data");
    kernel_ut::RunGenData("./celu_v2_data", {"'(256)'", "float32", "1.0"});

    std::string path = kernel_ut::GetTestWorkDir();

    CeluV2TilingData* tilingData = reinterpret_cast<CeluV2TilingData*>(tiling);
    tilingData->smallCoreDataNum = static_cast<int64_t>(dataNum);
    tilingData->bigCoreDataNum = static_cast<int64_t>(dataNum);
    tilingData->tileDataNum = static_cast<int64_t>(dataNum);
    tilingData->smallTailDataNum = static_cast<int64_t>(dataNum);
    tilingData->bigTailDataNum = static_cast<int64_t>(dataNum);
    tilingData->finalSmallTileNum = 1;
    tilingData->finalBigTileNum = 1;
    tilingData->tailBlockNum = 0;
    tilingData->alpha = alpha;
    tilingData->invAlpha = invAlpha;

    ReadFile(path + "/celu_v2_data/input_x.bin", xByteSize, x, xByteSize);
    auto KernelCeluV2 = [](GM_ADDR x, GM_ADDR y, GM_ADDR workspace, GM_ADDR tiling) {
        ::celu_v2<0>(x, y, workspace, tiling);
    };
    ICPU_SET_TILING_KEY(0);
    AscendC::SetKernelMode(KernelMode::AIV_MODE);
    ICPU_RUN_KF(KernelCeluV2, blockDim, x, y, workspace, (uint8_t*)(tilingData));
    WriteFile(path + "/celu_v2_data/output.bin", y, yByteSize);

    AscendC::GmFree(x);
    AscendC::GmFree(y);
    AscendC::GmFree(workspace);
    AscendC::GmFree(tiling);
}

TEST_F(celu_v2_test, test_case_fp16_alpha1)
{
    size_t dataNum = 256;
    float alpha = 1.0f;
    float invAlpha = 1.0f / alpha;
    size_t xByteSize = dataNum * sizeof(uint16_t);
    size_t yByteSize = dataNum * sizeof(uint16_t);
    size_t tiling_data_size = sizeof(CeluV2TilingData);

    uint8_t* x = (uint8_t*)AscendC::GmAlloc(xByteSize);
    uint8_t* y = (uint8_t*)AscendC::GmAlloc(yByteSize);
    uint8_t* workspace = (uint8_t*)AscendC::GmAlloc(16 * 1024 * 1024);
    uint8_t* tiling = (uint8_t*)AscendC::GmAlloc(tiling_data_size);
    uint32_t blockDim = 1;
    kernel_ut::SetupTestEnvironment("activation/celu_v2/tests/ut/op_kernel/celu_v2_data", "celu_v2_data");
    kernel_ut::RunGenData("./celu_v2_data", {"'(256)'", "float16", "1.0"});

    std::string path = kernel_ut::GetTestWorkDir();

    CeluV2TilingData* tilingData = reinterpret_cast<CeluV2TilingData*>(tiling);
    tilingData->smallCoreDataNum = static_cast<int64_t>(dataNum);
    tilingData->bigCoreDataNum = static_cast<int64_t>(dataNum);
    tilingData->tileDataNum = static_cast<int64_t>(dataNum);
    tilingData->smallTailDataNum = static_cast<int64_t>(dataNum);
    tilingData->bigTailDataNum = static_cast<int64_t>(dataNum);
    tilingData->finalSmallTileNum = 1;
    tilingData->finalBigTileNum = 1;
    tilingData->tailBlockNum = 0;
    tilingData->alpha = alpha;
    tilingData->invAlpha = invAlpha;

    ReadFile(path + "/celu_v2_data/input_x.bin", xByteSize, x, xByteSize);
    auto KernelCeluV2 = [](GM_ADDR x, GM_ADDR y, GM_ADDR workspace, GM_ADDR tiling) {
        ::celu_v2<0>(x, y, workspace, tiling);
    };
    ICPU_SET_TILING_KEY(0);
    AscendC::SetKernelMode(KernelMode::AIV_MODE);
    ICPU_RUN_KF(KernelCeluV2, blockDim, x, y, workspace, (uint8_t*)(tilingData));
    WriteFile(path + "/celu_v2_data/output.bin", y, yByteSize);

    AscendC::GmFree(x);
    AscendC::GmFree(y);
    AscendC::GmFree(workspace);
    AscendC::GmFree(tiling);
}
